-- roblox/src/utils/data_storage.lua
local DataStorage = {}

function DataStorage.Save(key, value)
    -- Save data locally or to a remote storage
end

function DataStorage.Load(key)
    -- Load data from local or remote storage
    return nil
end

return DataStorage
