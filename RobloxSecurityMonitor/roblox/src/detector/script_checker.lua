-- roblox/src/detector/behavior_monitor.lua
local Logger = require(script.Parent.config)

local BehaviorMonitor = {}

function BehaviorMonitor.StartMonitoring()
    Logger:Log("Behavior monitor started.")
    -- Add logic to monitor player behaviors
end

return BehaviorMonitor
